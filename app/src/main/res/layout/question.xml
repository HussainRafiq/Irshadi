<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
android:layout_margin="10dp"
    android:background="@drawable/bgpanel"
    android:orientation="vertical"
    android:layout_height="wrap_content">
    <TextView
        android:layout_width="match_parent"
        android:background="@android:color/transparent"
        android:layout_height="wrap_content"
        android:text="She agreed with her friends that whoever got the highest grades would buy chocolate for the others; is that permissible?"
        android:textStyle="bold"
        android:textSize="12dp"
        android:layout_margin="5dp"
        android:textColor="@color/colorPrimaryDark"
        />
    <TextView
        android:layout_width="match_parent"
        android:background="@android:color/transparent"
        android:layout_height="wrap_content"
        android:text="She agreed with her friends that whoever got the highest grades would buy chocolate for the others; is that permissible She agreed with her friends that whoever got the highest grades would buy chocolate for the others; is that permissible She agreed with her friends that whoever got the highest grades would buy chocolate for the others; is that permissible?"
        android:textStyle="italic"
        android:textSize="12dp"
        android:layout_marginStart="5dp"
        android:layout_marginEnd="5dp"
        android:layout_marginBottom="5dp"
        android:textColor="@android:color/black"
    />

</LinearLayout>
